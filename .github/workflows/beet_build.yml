name: Push Data Pack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install beet bolt mecha
    - name: Beet Build
      run: beet -p beet-release.yml build
          
    # - name: 📂 Sync files
    #   uses: SamKirkland/FTP-Deploy-Action@3.1.1
    #   with:
    #     ftp-server: sftp://panel.newrealms.us/
    #     ftp-username: iredsc.378e2546
    #     ftp-password: ${{ secrets.password }}
    #     port: 2022
    #     local-dir: ./build/release/
    #     server-dir: ./world/datapacks/

    - name: 📂 Sync files
      uses: Atyn/Simple-SFTP@0.0.2
      with:
        ftp-server: sftp://iredsc.378e2546:${{ secrets.password }}@panel.newrealms.us:2022/world/datapacks
        local-directory: ./build/release/
